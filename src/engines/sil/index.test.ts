import { describe, it, expect } from 'vitest'
import { computeSilAccrual } from './index'

describe('computeSilAccrual', () => {
  it('is a pure function placeholder', () => {
    expect(() => computeSilAccrual({} as never)).toThrow('not yet implemented')
    // TODO: replace with Labor Code Art. 95 scenario tests once engine is implemented
  })
})
